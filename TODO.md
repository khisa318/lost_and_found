# TODO - Lost & Found website pages

- [x] Implement `src/pages/Navbar.jsx` with navigation links to `/`, `/items`, `/about`, `/admin`.
- [x] Implement `src/pages/Home.jsx` as landing page for lost & found (hero + CTAs + how it works).
- [x] Implement `src/pages/Items.jsx` with browse/search UI and sample listings for lost/found items.
- [x] Implement `src/pages/About.jsx` describing how the site works.
- [x] Implement `src/pages/Admin.jsx` with an admin dashboard layout (placeholder cards).
- [x] (after edits) Run `npm run dev` and verify routes render: `/`, `/items`, `/about`, `/admin`.



